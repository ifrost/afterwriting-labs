define(function(require) {

    var Protoplast = require('protoplast');

    var FakeGapiAuth = Protoplast.extend({
        
        init: function() {
            
        }
        
    });

    return FakeGapiAuth;
});